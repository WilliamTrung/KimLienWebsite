namespace KL_SP_MappingConfig
{
    public class AutoMapperAssembly
    {
        //Add AutoMapper profiles into the same namespace so that it can be injected automatically by adding in Program API
        //builder.Services.AddAutoMapper(Assembly.GetAssembly(typeof(KL_SP_AutoMapperConfig.SP_AutoMapperAssembly)));
    }
}